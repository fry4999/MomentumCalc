# Future Tool Modules

This folder is reserved for future client-only mechanism tools.

When calculators are added, keep them as browser modules with pure calculation
functions and small UI mount functions. They should not use server APIs,
cookies, accounts, databases, or browser storage unless that project direction
explicitly changes.
